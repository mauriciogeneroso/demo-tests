spring:
  jmx:
    enable: false
  application:
    name: parallel-ft
  profiles:
    active: local

service:
  host: http://localhost:8080
  context-path: /demo

wiremock:
  host: localhost
  port: 9090